spring.application.name=demo

# Enable DEBUG logging for everything
logging.level.root=DEBUG
logging.level.com.example.demo=DEBUG
logging.level.org.springframework=DEBUG
logging.level.org.hibernate=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# MySQL datasource (for NORMAL app)
spring.datasource.url=jdbc:mysql://localhost:3306/inventory_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=tanuri123***

# JPA / Hibernate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# TEST PROFILE (activates H2 for tests)
spring.profiles.active=test